import apiHelpers, { fetchResidents } from './apiHelpers';

describe('apiHelpers', () => {
  let mockUrl = 'https:/www.swapi.com/fake';
  let mockPeople = [{homeworld: mockURL}, ]

  describe('fetchResidents', () => {
    it('should ')
  })
})